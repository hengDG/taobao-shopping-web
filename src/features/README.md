Each folder in this directory is a business domain.

Keep it simple:

1. Start with only `index.ts`.
2. Add `components`, `hooks`, or `services` only when needed.
3. Keep route-specific UI in `app`, and business logic in `features`.
